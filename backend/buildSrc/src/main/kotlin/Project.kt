object Project {
    const val app = ":app"

    object Authentication {
        const val data = ":authentication-data"
        const val domain = ":authentication-domain"
    }

    const val core = ":core"

    object Figure {
        const val data = ":figure-data"
        const val domain = ":figure-domain"
    }

    object User {
        const val data = ":user-data"
        const val domain = ":user-domain"
    }
}
